pyswisseph==2.10.3.2
fastapi>=0.104.0
uvicorn>=0.24.0
kerykeion>=5.12,<6   # 06.09: на 5.x проверено всё; на 4.x нет фабрики с секундами и разметки для скрытия домов
geopy>=2.4.0
timezonefinder>=6.2.0
pytz>=2023.3
pydantic>=2.0.0
httpx>=0.25.0
python-multipart>=0.0.6
websockets>=12.0
edge-tts>=6.1.0
# === Голосовой Квантареон (перенесён с Амверы 01.08.2026) ===
aiohttp>=3.9.0
# === Интернет для голоса (06.08.2026) ===
ddgs>=9.0.0
# === Счётчик посещений: хранение в Cloudflare R2 (08.08.2026) ===
boto3>=1.34.0
# === Падежи городов для погоды (16.08.2026) ===
# «погода в Самаре» справочник Open-Meteo читал как чужое имя и находил
# Самар на Филиппинах — он не склоняет. Анализатор приводит слово к
# именительному падежу и помечает географические названия, отличая
# «Гори» (город) от «гори» (глагол). 16 МБ на диске, 38 МБ памяти.
pymorphy3>=2.0.0
pymorphy3-dicts-ru>=2.4.0
# === Smart Turn: модель, слушающая интонацию (27.08.2026) ===
# Решает, договорил человек или взял паузу — по звуку, а не по тексту.
# Сама модель лежит в папке turn/ рядом с voice.py, 8 МБ.
# Не поставится — движок работает по старой схеме, просто медленнее
# собирает длинную мысль.
onnxruntime>=1.17.0
transformers>=4.36.0
numpy>=1.24.0
